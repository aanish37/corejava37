package Collection;

public class CollectionDocs {
	/*
	 * -------------Java Collection/collection Framework---------------------
	 * 
	 * # use to store and processing dynamic multiple data/objects in java.
	 * 
	 * 1> list : dynamic array
	 *        a> ArrayList  : Slow processing, better to use for data storage.
	 *        b> LinkedList : fast processing, better to use for data processing.
	 * 2> set  : collection of unique of data/objects.
	 *        a> HashSet    : unique data only
	 *        b> TreeSet    : unique + sorted data 
	 *        c> LinkedHashSet : unique + insertion in order
	 * 3> Map  : data manage in key value pair.
	 *        a> HashMap       :<k,v> pair
	 *        b> TreeMap       :<k,v> + Sorted data
	 *        c> LinkedHashMap :<k,v> + insertion in data
	 *        
	 *        //q1> create list of book
	 *        //q2> create list of dog
	 */

}
