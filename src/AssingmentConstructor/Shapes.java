package AssingmentConstructor;
/*
 * static class TriangleNoParams {
        int a, b, c;

        TriangleNoParams() {
            this.a = 3;
            this.b = 4;
            this.c = 5;
        }

        int getPerimeter() {
            return a + b + c;
        }

        double getArea() {
            double s = getPerimeter() / 2.0;
            return Math.sqrt(s * (s - a) * (s - b) * (s - c));
        }
    }

    static class TriangleWithParams {
        int a, b, c;

        TriangleWithParams(int a, int b, int c) {
            this.a = a;
            this.b = b;
            this.c = c;
        }

        int getPerimeter() {
            return a + b + c;
        }

        double getArea() {
            double s = getPerimeter() / 2.0;
            return Math.sqrt(s * (s - a) * (s - b) * (s - c));
        }
    }

    static class Rectangle {
        int length, breadth;

        Rectangle(int length, int breadth) {
            this.length = length;
            this.breadth = breadth;
        }

        int getArea() {
            return length * breadth;
        }
    }
 */
class Shapes {
    
}
